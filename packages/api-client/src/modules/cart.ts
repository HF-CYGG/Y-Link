/**
 * 服务端购物车尚未完成业务与并发 Contract 设计。
 * 本文件只保留显式占位类型，禁止页面把本地 Store 当作服务端接口。
 */
export interface ServerCartContractPlaceholder {
  readonly status: 'not-designed'
  readonly followUp: 'server-cart-contract'
}
