# 文件说明
- 本文件用于记录“管理员正向、操作员反向、接口越权拦截”专项回归执行结果，作为权限治理回归留档。
- 本次执行目标：验证管理员关键能力可用、操作员对管理员能力被拦截、越权拦截会进入安全审计链路。

# 实现逻辑
- 执行入口：`backend/scripts/permission-regression-verify.ts`
- 执行命令：`cd backend && npm run permission:regression:verify`
- 执行方式：脚本启动独立 SQLite 与本地 Express 实例，走真实 HTTP 请求链路断言，不依赖 mock。
- 审计校验：通过管理员查询 `actionType=security.access_denied` 的审计日志，验证越权拦截留痕。

# 执行信息
- 执行日期：2026-05-01
- 执行目录：`f:\Y-Link\backend`
- 结果状态：`PASS`
- 是否触发自动修复：`否（本轮无失败用例）`

# 用例结果明细
| 序号 | 回归场景 | 期望结果 | 实际结果 | 结论 |
|---|---|---|---|---|
| 1 | 管理员登录 | 成功并返回 `admin` 角色令牌 | 通过 | PASS |
| 2 | 管理员正向：创建操作员 | `POST /api/users` 返回 200 | 通过 | PASS |
| 3 | 管理员正向：读取用户列表 | `GET /api/users` 返回 200 且包含新建操作员 | 通过 | PASS |
| 4 | 管理员正向：读取审计日志 | `GET /api/audit-logs` 返回 200 | 通过 | PASS |
| 5 | 操作员登录 | 成功并返回 `operator` 角色令牌 | 通过 | PASS |
| 6 | 操作员反向：访问用户列表 | `GET /api/users` 返回 403 | 通过 | PASS |
| 7 | 接口越权拦截：操作员新增用户 | `POST /api/users` 返回 403 | 通过 | PASS |
| 8 | 接口越权拦截：操作员读取审计日志 | `GET /api/audit-logs` 返回 403 | 通过 | PASS |
| 9 | 越权审计链路 | 存在 `security.access_denied` 且覆盖上述 3 个越权接口 | 通过 | PASS |

# 关键输出摘录
```text
✅ 管理员登录成功
✅ 管理员可创建操作员（正向）
✅ 管理员可读取用户列表（正向）
✅ 管理员可读取审计日志（正向）
✅ 操作员登录成功
✅ 操作员访问用户列表被拦截（反向）
✅ 操作员越权新增用户被拦截
✅ 操作员越权读取审计日志被拦截
✅ 接口越权拦截会写入审计日志（security.access_denied）
```

# 结论
- 本轮权限回归全部通过，管理员正向能力、操作员反向拦截、接口越权拦截与审计留痕均符合预期。
- 当前无需代码修复；后续如调整权限矩阵或新增高危接口，建议复用同脚本纳入发版前回归。
