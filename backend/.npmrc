; 模块说明：后端独立上下文 npm 配置
; 文件职责：统一 backend 子项目在独立 CI、Docker 构建与本地后端安装时使用官方 npm 源
; 实现逻辑：
; 1. backend Dockerfile 仅复制 `backend/package*.json`，因此需要在子目录单独提供 `.npmrc`；
; 2. 显式固定官方 registry，保证 `npm ci` 与 `npm audit` 命中 npm 官方审计服务。
registry=https://registry.npmjs.org/
