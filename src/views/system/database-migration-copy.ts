/**
 * 文件说明：数据库迁移文案常量。
 * 文件职责：统一收口系统配置页与数据库迁移助手页面共用的标题、步骤说明、重启提示与快捷入口文案，避免同一含义在多个页面出现不同说法。
 * 实现逻辑：
 * 1. 将“数据库迁移助手”统一设为页面与快捷入口的标准名称，减少用户在系统治理页面中的识别成本。
 * 2. 将“写入运行时覆盖后，需要重启后端服务才会生效”设为核心提示，确保关键风险提示在不同页面保持一致。
 * 3. 将推荐操作闭环抽成常量，便于后续系统配置页、工作台快捷入口或其他治理页直接复用。
 */

/**
 * 数据库迁移能力统一名称：
 * - 页面标题、快捷入口、说明卡片都应优先复用该名称；
 * - 避免同一功能在不同位置出现“助手 / 向导”混用。
 */
export const DATABASE_MIGRATION_ASSISTANT_NAME = '数据库迁移助手'

/**
 * 数据库迁移页简介：
 * - 用于页面首屏描述；
 * - 重点强调统一入口与降低手工改配置风险。
 */
export const DATABASE_MIGRATION_PAGE_DESCRIPTION =
  '建议统一通过数据库迁移助手完成 SQLite 到 MySQL 的预检、迁移、写入运行时覆盖、重启与回退闭环，减少手工改环境变量或覆盖文件带来的风险。'

/**
 * 推荐闭环步骤：
 * - 用于页面风险提醒、步骤说明和系统配置页快捷入口提示；
 * - 保证不同页面对“正确顺序”的描述完全一致。
 */
export const DATABASE_MIGRATION_RECOMMENDED_FLOW_TEXT =
  '执行预检 -> 创建迁移任务 -> 执行迁移 -> 写入运行时覆盖 -> 重启后端服务 -> 回到数据库迁移助手确认当前实际生效数据库'

/**
 * 重启生效提示：
 * - 切换、回退、清空覆盖后都要复用该说明；
 * - 统一强调“写入覆盖”和“真正生效”之间仍隔着一次后端重启。
 */
export const DATABASE_MIGRATION_RESTART_EFFECT_TEXT = '写入运行时覆盖后，需要重启后端服务才会生效。'

/**
 * 系统配置页快捷入口标题：
 * - 保持按钮文案简短直接；
 * - 让管理员一眼知道可以从系统配置页直达迁移助手。
 */
export const DATABASE_MIGRATION_ENTRY_LABEL = '进入数据库迁移助手'

/**
 * 系统配置页快捷入口补充说明：
 * - 告诉管理员数据库迁移、切换与回退都应从统一入口进入；
 * - 直接回应“减少查找成本”的需求。
 */
export const DATABASE_MIGRATION_ENTRY_DESCRIPTION =
  '数据库迁移、切换与回退请统一从这里进入，减少在左侧菜单中来回查找。'

/**
 * 页面默认切换原因：
 * - 统一描述“数据库迁移助手写入 MySQL 运行时覆盖”；
 * - 便于审计日志与页面提示保持同一口径。
 */
export const DATABASE_MIGRATION_SWITCH_REASON_DEFAULT = '数据库迁移助手写入 MySQL 运行时覆盖'

/**
 * 页面默认回退原因：
 * - 与切换原因形成对称口径；
 * - 便于后续检索数据库治理审计日志。
 */
export const DATABASE_MIGRATION_ROLLBACK_REASON_DEFAULT = '数据库迁移助手写入 SQLite 运行时覆盖'

/**
 * 清空运行时覆盖成功提示：
 * - 成功后立即告诉用户下一步动作；
 * - 避免只看到“已清空”却误以为已经自动切回默认环境。
 */
export const DATABASE_MIGRATION_CLEAR_OVERRIDE_SUCCESS =
  '已清空数据库运行时覆盖；如需回到默认环境配置，请重启后端服务后再回来确认“当前实际生效数据库”。'
