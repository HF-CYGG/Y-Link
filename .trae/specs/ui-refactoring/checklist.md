- [x] `style.css` 已更新，非遗青主色调和全局背景/字体设置生效。
- [x] 系统中不再存在大面积的深色背景（如 `bg-slate-900` 等）。
- [x] 所有内容区块均采用了白底、圆角（xl）、微弱阴影和细边框的卡片式设计。
- [x] Dashboard 页面的欢迎 Banner 渲染正确，数据卡片为明亮风格。
- [x] 出库单列表页的搜索条件和数据表格被分离在不同的视觉卡片中。
- [x] 基础资料页的结构已优化，符合卡片式布局标准。
- [x] 开单页具有带左侧非遗青色小竖条的区块标题，层次分明。
- [x] 页面无数据时的空状态均使用 `el-empty`，且视觉协调（有白色卡片背景包裹等）。
