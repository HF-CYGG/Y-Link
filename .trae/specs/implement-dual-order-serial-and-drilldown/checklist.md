- [x] 双类型订单规则已生效：`department` 与 `walkin` 可正常创建并区分
- [x] 部门单号按 `hyyzjd` 前缀连续递增，散客单号按 `hyyz` 前缀连续递增
- [x] 并发创建同类型订单时无重复单号、无串号
- [x] `orders` 新增字段已落库并在创建/详情/列表链路可见
- [x] `issuerName` 默认当前登录人且支持改为值班人员姓名
- [x] Dashboard 三个饼图可正常加载，统计口径支持订单类型筛选
- [x] Top5 榜单点击后可在抽屉查看明细（单号、时间、数量）
- [x] 标签维度支持时间范围聚合，能正确返回 `totalQuantity` 与 `totalAmount`
- [x] 凭证模板可预览，`window.print()` 打印输出版式可用
- [x] 系统配置中双流水参数仅管理员可编辑，普通角色不可修改
- [x] 配置变更具备审计记录（操作者、时间、变更内容）
- [x] 历史订单兼容展示正常，新规则仅影响新建订单
- [x] 迁移脚本执行通过且可回滚，关键索引与约束无冲突
- [x] 端到端回归通过，且性能验证 `npm run verify:performance` 通过
