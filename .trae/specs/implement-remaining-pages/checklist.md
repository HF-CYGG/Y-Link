* [x] 前端 `tag.ts`、`product.ts`、`order.ts` 接口定义已完备且能正常通信。

* [x] 后端已提供并调试通工作台（Dashboard）所需的统计数据 API。

* [x] 基础资料页能正常新增、编辑产品，且产品可成功绑定并展示多个标签。

* [x] 基础资料页能正常管理标签字典。

* [x] 出库单列表页可按 `show_no` 搜索历史单据。

* [x] 在出库单列表点击单据能正确弹出包含明细行的详情抽屉。

* [x] 工作台（Dashboard）卡片数据可动态渲染，快捷入口导航有效。

* [x] PC端和移动端在上述 3 个新页面均能正确响应式适配，无横向溢出破版。

* [x] 页面在无数据、加载中等状态下具有骨架屏或优雅的空状态插画提示。

* [x] 所有交互按钮均已处理防重复提交（Loading 态）。

