* [x] Dashboard 页面最外层不再是白色大卡片，内部 Widget 直接悬浮在底层灰色画布上。

* [x] `AppPageContainer` 组件不再强制提供白色底板，使得页面内部可以自由排布多个独立卡片。

* [x] `.apple-card` 的阴影不再过度发散，看起来更扎实，边界更清晰。

* [x] 亮色模式下，卡片的边框颜色足以勾勒出卡片的物理形状，避免与背景融为一体。

* [x] 其他核心业务页面（如出库开单、出库单列表）也没有出现“白套白”的层级混乱现象。

