* [x] `style.css` 已更新，全局 0.5s 柔和变色过渡生效，切换亮暗模式时无闪屏。

* [x] 亮色模式下底层画布（`body`）颜色变深，白色卡片具有明显的悬浮边界感。

* [x] 暗色模式下底层为深碳黑，卡片拥有极细的半透明白边框，层次清晰。

* [x] `AppLayout.vue` 中侧边栏和 Header 的背景色及边框已与 `.apple-card` 的新视觉规范对齐。

* [x] 顶部的亮暗切换按钮已替换为手写 Toggle。

* [x] Toggle 按钮的太阳/月亮图标在切换时淡入淡出且带有缩放动效。

* [x] Toggle 滑块在滑动时带有明显的物理回弹效果（`cubic-bezier` 生效）。

