# Y-Link 工作台、报表与出库链路

## 适用范围

- 适用于 Dashboard、排行榜、报表中心、出库开单、出库列表、订单删除/恢复/永久删除治理。
- 适用于查“为什么这个统计口径是这样、为什么单号回拨、为什么 O2O 关联订单一删会联动”的问题。

## 最后核对来源

- `src/views/dashboard/*`
- `src/views/order-entry/*`
- `src/views/order-list/*`
- `src/views/reports/*`
- `src/api/modules/dashboard.ts`
- `src/api/modules/order.ts`
- `src/api/modules/report.ts`
- `backend/src/routes/dashboard.routes.ts`
- `backend/src/routes/order.routes.ts`
- `backend/src/routes/report.routes.ts`
- `backend/src/services/dashboard.service.ts`
- `backend/src/services/order.service.ts`
- `backend/src/services/report.service.ts`

## 真实入口

- 管理端页面入口：`/dashboard`、`/order-entry`、`/order-list`、`/reports`
- 后端服务真源：`dashboard.service.ts`、`order.service.ts`、`report.service.ts`

## 前端链路

- Dashboard 页面显示四宫格、近 7 日趋势、Top 产品、Top 客户、最近动态，并支持钻取抽屉。
- 出库开单页通过商品检索、数量编辑、汇总卡片和提交动作构造 `SubmitOrderPayload`。
- 出库列表页负责筛选、详情抽屉、删除、恢复、永久删除以及合规标记编辑。
- 报表中心按报表类型切换字段定义和导出行为，不同报表共用一个前端 API 模块。

## 后端链路

- `dashboard.service.ts` 负责：
  - 今日/本月统计
  - 趋势聚合
  - Top 产品/客户
  - 最近动态
  - 产品/客户/标签钻取
- `order.service.ts` 负责：
  - 幂等提交
  - 主单/明细事务写入
  - 单号生成
  - 产品默认价快照回写
  - 软删除/恢复/永久删除
  - O2O 关联单据可见性联动
  - 流水号安全回拨
- `report.service.ts` 负责：
  - 库存、标签销售、金蝶、散客、出库流水等报表类型
  - 字段定义、分页查询、导出 Excel
  - 部门单与散客单字段差异兜底

## 关键状态/字段/快照

- 出库单主字段：`showNo`、`orderType`、`customerDepartmentName`、`customerName`、`totalQty`、`totalAmount`、`isDeleted`。
- 详情明细以快照优先：`productNameSnapshot`、`lineAmount`，避免商品后来改名影响历史单据展示。
- O2O 关联出库单通过 `idempotencyKey` 前缀识别，与预订单删除可见性联动。
- 报表字段集不是页面随意拼接，而是后端字段定义驱动。

## 权限与安全边界

- Dashboard 依赖 `dashboard:view`。
- 出库开单依赖 `orders:create`，查看历史依赖 `orders:view`，删除依赖 `orders:delete`，编辑合规位依赖 `orders:update`。
- 永久删除属于高风险操作，除了权限外还要求永久删除密码。

## 常见异常与排查顺序

1. 工作台数字不对：先查 `dashboard.service.ts` 的过滤条件是否排除了软删除单据。
2. 趋势图或排行榜和列表不一致：查 Dashboard 与报表是否使用了不同时间范围或订单类型过滤。
3. 出库单重复提交：查 `idempotencyKey` 是否缺失或前端重复生成策略异常。
4. 删除后流水号回拨异常：查 `order.service.ts` 的 `recalibrateCurrentFromOccupancy()`。
5. O2O 关联出库单删除后预订单看不见/看得见不对：查关联可见性同步逻辑。

## 验证与回归关注点

- 出库链路至少回归：新建订单、详情查看、软删、恢复、永久删、合规标记编辑。
- 工作台回归：四宫格、趋势、钻取、近期动态文案。
- 报表回归：报表类型切换、字段列头、分页、导出文件名。
