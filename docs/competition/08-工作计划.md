# Y-Link——智能库存与订单协同管理系统工作计划

## 1. 基本信息

| 项目 | 内容 |
| --- | --- |
| 作品名称 | Y-Link——智能库存与订单协同管理系统 |
| 工作基线 | `origin/main`，`b149f945ad4702bc56487955ac2e2a0b8e359e2a` |
| 参赛范围 | 管理端 Web、客户端 Web/H5、供应端 Web、后端、数据库、Docker |
| 团队成员/指导教师/周期 | 待人工填写 |

## 2. 工作分解

| 阶段 | 交付 | 状态 |
| --- | --- | --- |
| 范围与资料核对 | 代码基线、模块边界、材料源 | 已整理 |
| 构建与脚本验证 | 编码、构建、onebox、发布、迁移、并发、性能记录 | 已执行，存在失败项 |
| 本地演示准备 | 合成数据、独立账号、可恢复数据卷、浏览器素材 | 已完成；Docker 保持运行并保留数据 |
| 作品制作 | 11 份 DOCX、B/C PDF、16 页 PPTX、V2 MP4 | 已完成 |
| 交付复核 | 源码白名单、敏感扫描、大小、哈希、渲染检查 | 已完成；平台字段待人工确认 |

## 3. 当前验证记录

| 检查项 | 结果 |
| --- | --- |
| 编码、Web 构建、后端构建 | 通过 |
| onebox 冒烟 | 首次端口瞬时占用后换 `18081/3311` 通过 |
| 发布回归 | 失败：供应送货提交 HTTP 400 `Required` |
| 数据库迁移 | 失败：夹具触发 `ck_account_lifecycle_event_type` |
| 数据库并发 | 失败：“请选择到店取货时间” |
| 性能 | 失败：`OrderEntryView`、`OrderListView` 超出预算 |

## 4. 交付记录

- 作品材料已生成：11 份 DOCX、`B_Y-Link设计文档.pdf`、`C_Y-Link创作过程资料.pdf`、白名单源码 ZIP、16 页 PPTX 和 V2 MP4。
- PPTX 为 1,567,192 B，SHA-256 记录为 `6DA4B8AC...AFBBA6`；MP4 为 22,993,451 B，290 秒，H.264/AAC、1920×1080、30 fps，SHA-256 记录为 `53943EFC...9DACCA`。
- 浏览器素材包含 33 张正式图和 13 帧交互素材，均为 Chrome 原生 viewport；Docker 健康检查通过，容器和数据卷保留供复查。
- `verify:release`、`verify:db:concurrency`、`verify:db:migration`、`verify:performance` 仍为失败，未改写为通过。

## 5. 人工收尾

团队信息、指导教师、联系方式、正式生产 URL、评审期 Web 链接和测试账号，以及四项失败验证的处置决定，待人工确认。
