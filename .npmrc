; 模块说明：仓库级 npm 配置
; 文件职责：统一 Y-Link 根项目在本地、CI 与 Docker 前端构建阶段使用官方 npm 源
; 实现逻辑：
; 1. 显式声明 registry 为官方源，避免环境默认镜像导致 lockfile 与 audit 口径不一致；
; 2. 让根目录 `npm ci`、`npm audit` 与前端 Docker 构建共享同一下载来源。
registry=https://registry.npmjs.org/
